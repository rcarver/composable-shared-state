import XCTest
@testable import composable_scoped_state

final class composable_scoped_stateTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(composable_scoped_state().text, "Hello, World!")
    }
}
